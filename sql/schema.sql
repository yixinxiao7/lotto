CREATE TABLE combinations(
  date VARCHAR(20) NOT NULL,
  val1 INT NOT NULL,
  val2 INT NOT NULL,
  val3 INT NOT NULL,
  val4 INT NOT NULL,
  val5 INT NOT NULL,
  model VARCHAR(5) NOT NULL,
  PRIMARY KEY(date)
);
